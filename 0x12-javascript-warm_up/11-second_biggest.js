#!/usr/bin/node

const len = process.argv.length

if (len <= 3) console.log(0);
else {
    let biggest = parseInt(process.argv[2]);
    let sBiggest = parseInt(process.argv[3]);
    if (
    for (let i = 0; i < len; ++i) {
	
